/**
 * * This hook returns the breakpoints for responsiveness
 */
export const useBreakpoint = () => {
  const breakpoint = {
    sm: 576,
    md: 768,
    lg: 992,
    xl: 1200
  }

  return breakpoint
}